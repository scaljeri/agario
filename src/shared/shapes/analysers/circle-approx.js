import BaseAnalyser from './base-analyser';

export default class CircleApprox extends BaseAnalyser {
    constructor(ARGBA) {
        super(ARGBA);
    }

    start(x, y) {
        console.log('circle apporx');

    }
}
